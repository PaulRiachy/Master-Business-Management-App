"use client";import {useEffect,useState} from "react";import {api} from "@/lib/api";
export default function Suppliers(){const [rows,setRows]=useState<any[]>([]),[form,setForm]=useState({name:"",contact:"",country:"",payment_terms:"Net 30"}),[error,setError]=useState("");async function load(){try{setRows(await api<any[]>("/suppliers"))}catch(e:any){setError(e.message)}}useEffect(()=>{load()},[]);async function add(e:React.FormEvent){e.preventDefault();try{await api("/suppliers",{method:"POST",body:JSON.stringify(form)});setForm({name:"",contact:"",country:"",payment_terms:"Net 30"});load()}catch(e:any){setError(e.message)}}return <main className="container stack"><div><h1>Suppliers</h1><p className="muted">Supplier master data. Visible to Admin and Procurement.</p></div>{error&&<div className="error">{error}</div>}<form className="card grid two" onSubmit={add}><label>Name<input className="input" required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Contact<input className="input" value={form.contact} onChange={e=>setForm({...form,contact:e.target.value})}/></label><label>Country<input className="input" value={form.country} onChange={e=>setForm({...form,country:e.target.value})}/></label><label>Payment Terms<input className="input" value={form.payment_terms} onChange={e=>setForm({...form,payment_terms:e.target.value})}/></label><button className="btn">Add supplier</button></form><div className="card"><table className="table"><thead><tr><th>Name</th><th>Contact</th><th>Country</th><th>Payment Terms</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.name}</td><td>{r.contact}</td><td>{r.country}</td><td>{r.payment_terms}</td></tr>)}</tbody></table></div></main>}
