"use client";
import {useState} from "react"; import {useRouter} from "next/navigation"; import {api} from "@/lib/api"; import {ThemeToggle} from "@/components/theme-toggle";
export default function Login(){const [email,setEmail]=useState("admin@example.com"),[password,setPassword]=useState("admin123"),[error,setError]=useState(""); const router=useRouter(); async function submit(e:React.FormEvent){e.preventDefault();setError("");try{const data=await api<any>("/auth/login",{method:"POST",body:JSON.stringify({email,password})});localStorage.setItem("mbm_token",data.access_token);localStorage.setItem("mbm_user",JSON.stringify(data.user));router.push("/dashboard")}catch(err:any){setError(err.message)}}return <main className="auth"><div className="auth-top"><ThemeToggle /></div><form className="card auth-card stack" onSubmit={submit}><div><h1>Master Business</h1><p className="muted">Internal project lifecycle MVP</p></div><label>Email<input className="input" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<div className="error">{error}</div>}<button className="btn">Sign in</button><div className="small muted">Demo: admin@example.com / admin123<br/>sales@example.com / sales123<br/>procurement@example.com / proc123</div></form></main>}
